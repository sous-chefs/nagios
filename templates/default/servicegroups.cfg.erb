# Service Group definitions
#
# Autogenerated by Chef.


# Service Groups defined by data bags
<% @servicegroups.each do |servicegroup| -%>
define servicegroup {
  servicegroup_name <%= servicegroup['servicegroup_name'] || servicegroup['description'] || servicegroup['id'] %>

  #per-databag overrides:
  <% %w{alias members servicegroup_members notes notes_url action_url}.each do |conf| %>
    <% unless servicegroup[conf].nil? %>
  <%= conf %> <%= servicegroup[conf] %>
    <% end %>
  <% end %>
}

<% end -%>
